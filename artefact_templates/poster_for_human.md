# Product One‑Pager / Poster

## 1. Title & Positioning

**Product name:**  
`<e.g. Sidecar>`

**Positioning line (one sentence):**  
`<e.g. A spatial learning layer for LLM chats>`

---

## 2. Target Customer & Context

**Primary customer:**  
`<e.g. Busy knowledge workers using LLMs daily>`

**Usage context (when/where):**  
`<e.g. During research sessions in ChatGPT on desktop>`

---

## 3. Customer Problem & Goal

**Problem (in customer’s words, 2–3 sentences):**  
`<What is painful, frustrating, slow, or risky today?>`

**Job-to-be-done (1 sentence):**  
`<When I … I want to … so that I can …>`

---

## 4. Solution Summary

**What the product does (3–4 sentences):**  
`<How it works at a high level and why it’s better than today’s behavior/alternatives.>`

---

## 5. User Journey & Key Use Cases

**User journey (3–5 steps, from entry to value):**

1. `<Entry point>`
    
2. `<Key action>`
    
3. `<Core experience>`
    
4. `<Outcome>`
    

**Primary use cases (max 3, bullet phrase each):**

- `<Use case 1 – who + what + why>`
    
- `<Use case 2 – who + what + why>`
    
- `<Use case 3 – who + what + why>`
    

---

## 6. Core Capabilities (Must‑Haves)

`<3–5 bullets, each “capability → value”>`

- `<Capability 1: description, so that user gets X value>`
    
- `<Capability 2: description, so that user gets Y value>`
    
- `<Capability 3: description, so that user gets Z value>`
    

---

## 7. Target Market, Scope & Constraints

**Initial target market / segment:**  
`<Who we launch to first?>`

**Scope for v1 (what’s in):**  
`<Short list or phrase>`

**Out of scope for v1 (what’s explicitly out):**  
`<Short list or phrase>`

**Key constraints (tech, compliance, platform, etc.):**  
`<Anything that shapes how we build or ship>`

---

## 8. Success Metrics

**Primary outcome metric (user/business impact):**  
`<e.g. Reduce time to X by Y%>`

**Key behavior metric(s):**

- `<e.g. % of users completing core journey>`
    
- `<e.g. retention / repeat usage in 7 or 30 days>`
    

---

## 9. Differentiation & Alternatives

**Today’s workaround / status quo:**  
`<What users do now instead?>`

**Main alternatives (tools or behaviors):**  
`<Short list>`

**Our edge (1–2 sentences):**  
`<Why this is meaningfully better/different for the target customer>`

---

## 10. Risks & Open Questions

`<Top 3–5 items, short bullets>`

- `<Risk / unknown 1>`
    
- `<Risk / unknown 2>`
    
- `<Risk / unknown 3>`
    

---

## 11. Ask

`<What decision or support you want from this poster?>`  
`<e.g. Approve v1 scope and resources for 4-week build + 2-week experiment>`